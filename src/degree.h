#ifndef DEGREE_H
#define DEGREE_H

// Enum type to represent each degree program
enum class DegreeProgram
{
   SECURITY,
   NETWORK,
   SOFTWARE
};
// Array of strings matching the order of degree programs specified in the enum type
extern std::string degreeProgramStrings[];

#endif
